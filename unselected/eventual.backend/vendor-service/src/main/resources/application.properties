spring.application.name=vendor-service
server.port=8086

# PostgreSQL — vendor-service owns the eventual_vendors database
spring.datasource.url=jdbc:postgresql://localhost:5432/eventual_vendors
spring.datasource.username=postgres
spring.datasource.password=postgres
