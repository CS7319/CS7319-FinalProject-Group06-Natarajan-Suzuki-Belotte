spring.application.name=support-service
server.port=8085

# PostgreSQL — support-service owns the eventual_support database
spring.datasource.url=jdbc:postgresql://localhost:5432/eventual_support
spring.datasource.username=postgres
spring.datasource.password=postgres
